.DS_Store
*.swp
*.swo
*~

# ─── 运行时产物（工作流结束即失效，不入库）───

# Agent Teams 运行时状态（进程ID、消息队列）
.codebuddy/teams/
.claude/teams/

# Plan 运行时产物（CodeBuddy/Claude 的任务计划缓存）
.codebuddy/plans/
.claude/plans/

# memory 文件是项目级经验记忆，应在业务项目仓库中积累提交
# 但在 ai-team 引擎仓库中应为空（引擎模板不带运行数据）
.codebuddy/memory/*.md
.claude/memory/*.md

# ─── 本地文档（博文草稿、笔记等，不入库）───
docs/
# 例外：工作流可视化产物（pre-commit hook 自动重生，进 git 便于团队同步）
!docs/
!docs/workflow-visualization.html

# ─── Python 运行缓存（scripts/ 工具链产生，不入库）───
__pycache__/
*.py[cod]
.pytest_cache/

# iWiki plugin files
.iwiki
.vscode/iwiki-*.json
