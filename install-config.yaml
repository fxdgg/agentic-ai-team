# AI Team 工作流文件清单
# shared（共享核心）+ platform-specific（平台特定）

shared:
  workflow-orchestrator:
    agents: "20+ Agent 行为规范"
    templates: "文档模板"
    references: "Schema 和参考文件"
    scripts: "辅助脚本"
    phases/output-formats: "展示格式模板"
    rules: "编码规范"
  standalone-skills: [docx, figma-d2c, model-router, pdf, quality-guardian, skill-creator, skill-learner, token-budget-manager]

platform-specific:
  orchestrator: SKILL.md
  commands: [evolve-apply, evolve, flow-import, flow-run, flow-status, flow-upgrade, guard, knowledge, team-init]
  phases: [analyse-product-rules, analyse-tech-rules, architect-backend-level1, architect-backend-level2, architect-backend-level3, architect-backend-rules, build-verify-rules, clarify-rules, implement-rules, import-rules, rollback-rules, visual-review-rules]
  standalone-skills: [capability-router, git-push-helper, knowledge-evolution, mcp-setup-guide, prd-creator, send-flow-message, tapd-toolkit, team-hub]
  rules:
    codebuddy-only: [tcb, anydev]
  claude-only: [CLAUDE.md, settings.json]
