# AI Team 使用说明

> 面向业务项目使用者的原理解读与操作手册。
> 架构细节以 [ARCHITECTURE.md](./ARCHITECTURE.md) 为权威源；本文件只回答「它是什么、怎么跑、能做什么、要不要拷到业务项目」。

---

## 1. 一句话定位

AI Team **不是独立平台、不是 CLI、不是 npm 包**。它是一套安装到业务项目里的 **IDE 原生工作流引擎**：用 `.codebuddy/`（CodeBuddy）和 `.claude/`（Claude Code）里的 Skill / Command / Agent / Rule 定义文件，驱动多个 AI Agent 按 16 阶段状态机完成「需求分析 → 架构设计 → 编码 → 验证 → 归档」。

**核心理念**：文件系统即状态机，团队知识持续沉淀，越用越聪明。

---

## 2. 要不要 copy 到目标项目？

**要。** 业务项目必须把引擎的运行时目录拷进去，IDE 才能识别命令和 Skill。

| 要不要拷 | 内容 | 原因 |
|---------|------|------|
| **必须拷** | `.codebuddy/` + `.claude/` | IDE 只认工作区里这两个目录；`/flow-run` 等命令、编排器、子 Agent 全在这里 |
| **不要拷** | `scripts/`、`meta/`、`ARCHITECTURE.md`、`CLAUDE.md`、`CODEBUDDY.md`、本仓库根文档 | 引擎维护者工具链与 DSL，运行时完全不读 |
| **不要当引擎拷** | 业务代码、`docs/`、`.ai-team/` | 这些是**业务项目自己生成**的，不是引擎源码 |

拷完即可用。团队其他成员只需同步这两个目录，不必 clone 整个 `ur-ai-team` 仓库。

后续升级用 `/flow-upgrade`（远程对比 + 选择性更新，双平台同步），不必再手工 `cp`。

---

## 3. 实现原理

### 3.1 它为什么能跑起来

CodeBuddy / Claude Code 会扫描工作区根目录下的：

- `.{platform}/commands/*.md` → 变成斜杠命令（如 `/flow-run`）
- `.{platform}/skills/*/SKILL.md` → 变成可触发的 Skill
- `.{platform}/skills/*/agents/*.md` → 被编排器按阶段调度的子 Agent
- `.{platform}/rules/` → 写入业务代码时遵守的编码规则

没有后台服务、没有数据库、没有运行时守护进程。Agent 读这些 Markdown / JSON，按协议写磁盘文件，状态就前进了。

### 3.2 三类仓库各司其职

```
┌─────────────────────┐     cp -r .codebuddy/ + .claude/     ┌──────────────────────────┐
│  ur-ai-team         │ ───────────────────────────────────► │  业务项目工作区           │
│  （引擎源码仓）      │                                      │  .codebuddy/ + .claude/  │
└─────────────────────┘                                      │  .ai-team/project.yaml   │
                                                             │  docs/workflows/         │
┌─────────────────────┐     /team-init 连接 + git pull/push  │  业务代码 repos[]        │
│  团队知识仓库        │ ◄──────────────────────────────────► │                          │
│  （独立 Git 仓）     │                                      └──────────────────────────┘
│  tech-wiki /        │
│  biz-wiki /         │
│  team-conventions   │
└─────────────────────┘
```

| 仓库 | 装什么 | 谁维护 |
|------|--------|--------|
| **引擎仓**（本仓库） | Skill / Command / Agent 定义 | 引擎维护者 |
| **业务项目仓**（1～N） | 业务代码 + 引擎副本 + 工作流产物 | 业务团队 |
| **团队知识仓**（1 个，团队共享） | 跨项目可复用的技术/业务知识 | 团队 maintainer |

不同业务项目可以连**同一个**知识仓库，这是跨项目复用的关键。

### 3.3 文件系统即状态机

每个需求在磁盘上有独立工作目录（路径由 `projectConfig.docsRoot` 解析，单仓一般是 `docs/workflows/{需求ID}/`）：

```
docs/workflows/{需求ID}/
├── state.json              ← 唯一状态源（当前阶段、历史、配置、意图）
├── analysis/               ← 产品/技术分析产物
├── architecture/           ← 前后端架构产物
├── implementation/         ← 实现记录
└── ...
```

- `state.json.currentPhase` 是当前阶段，更新前必须过流转守卫（`phase-transitions.json`）
- 阶段产物就是进度凭证；对话断了，下次 `/flow-run` 读 `state.json` 即可续跑
- 没有「工作流服务」可以重启——恢复点全在磁盘上

### 3.4 编排器不干活，只调度

核心 Skill 是 `workflow-orchestrator`。它**自己不做**分析、设计、编码、测试，只负责：

1. 读 `state.json`，判断当前阶段
2. 按阶段加载对应 `phases/*-rules.md`
3. 用 IDE 的 Task / Agent Teams 拉起专业子 Agent
4. 校验产物、更新状态、向用户做 Preview / Summary
5. 过流转守卫后进入下一阶段

子 Agent 在独立上下文里干活，只把**结构化结论**交回编排器（上下文防火墙，约 10 倍压缩），避免单个超长对话把上下文撑爆。

### 3.5 单仓 / 多仓用同一套模型

用 `repos[]` 描述代码仓，**零分支判断**：

- 单仓：`repos[]` 只有 1 个元素，`path: "./"`
- 多仓：N 个元素，每个有 `name` / `path` / `type`（`backend` / `frontend` / `docs` 等）

后续阶段一律遍历 `repos[]`。多仓时文档落在独立 `type=docs` 仓，`docsRoot` 由 INIT 自动解析，Agent 文档里仍写 `docs/...`，编排器运行时拼绝对路径。

### 3.6 知识按需查询，而不是全量灌进 Prompt

知识有三套正交维度：

| 维度 | 取值 |
|------|------|
| **存在哪** | Layer 0-P 个人偏好 · 0-T 团队约定 · 1 技术 wiki · 2 业务 wiki · 3 项目知识 |
| **是什么** | `model` / `decision` / `guideline` / `pitfall` / `process` |
| **多可信** | `draft` → `verified` → `proven`（双信号衰减：时间 + 代码事实校对） |

Agent 通过三级索引主动查阅（全景目录 → 分类清单 → 完整条目），每个阶段有独立查询预算。ARCHIVE 阶段提取新知识并判定是否提升到团队仓库，下次工作流自动受益。

### 3.7 失败了也能继续

| 机制 | 作用 |
|------|------|
| **三级降级** | L1 Agent Teams 并行 → L2 Task 串行 → L3 单体兜底 |
| **检查点** | 后端架构 4 步、产品分析 3 级，断点从最近检查点续 |
| **质量门禁** | `BUILD_VERIFY` 为 P0（失败可精细回退到 IMPLEMENT，只重跑失败端）；`VISUAL_REVIEW` 为 P1（无设计稿自动跳过） |
| **守卫模式** | `/guard` 开启后每步先确认再执行 |

---

## 4. 运行流程

### 4.1 从安装到第一次交付

```
业务项目打开 IDE
        │
        ▼
 拷贝 .codebuddy/ + .claude/     ← 只需这一次（或 /flow-upgrade 更新）
        │
        ▼
     /team-init                  ← 每个项目一次：连知识仓、填 repos[]
        │
        ▼
    /flow-import（可选）          ← 已有代码库：导入历史知识，建立基线
        │
        ▼
      /flow-run                  ← 日常：从 PRD / 文字 / 上下文启动
        │
        ▼
  16 阶段状态机（每阶段 Preview → Execute → Summary，人确认后才转）
        │
        ▼
      ARCHIVE → DONE             ← 知识沉淀、TAPD/企微归档
        │
        ▼
  /knowledge · /evolve           ← 维护知识、复盘流水线
```

### 4.2 `/flow-run` 启动分流

| 你怎么启动 | 系统怎么做 |
|-----------|-----------|
| `/flow-run docs/prd/xxx.md` 或 `@` 引用文件 / Figma 链接 | 解析文档（或走 D2C），确保有 PRD 后启动 |
| `/flow-run 做个广告预算告警` | 引导补全 PRD 后启动 |
| `/flow-run`（无参数） | 从已有 PRD 库选择，或新建 PRD |

首次运行（工作区还没有 `docs/`）会先问：导入已有项目 / 全新项目 / 已有 PRD 直接开始。

INIT 阶段会做：解析 PRD、加载 `repos[]`、注入知识仓库路径、探测 TAPD、跑 **IntentGate**（意图分类）。这是唯一不需要用户确认、自动流转的阶段。

IntentGate 五类意图会微调后续路由：

| 意图 | 流水线策略 |
|------|-----------|
| `new-feature` | 完整 16 阶段 |
| `feature-modify` | 完整流程，产品分析注入历史基线 |
| `bug-fix` | 可简化架构阶段，尽快到实现 |
| `tech-refactor` | 弱化产品分析，强化技术分析与架构 |
| `d2c-to-workflow` | 设计稿直通：前端实现可跳过（D2C 已完成），从构建验证起恢复正常 |

### 4.3 16 阶段状态机

```
INIT → ANALYSE_PRODUCT → CLARIFY_PRODUCT
     → ANALYSE_TECH → CLARIFY_TECH
     → ARCHITECT_BACKEND → CLARIFY_ARCH_BACKEND
     → ARCHITECT_FRONTEND → CLARIFY_ARCH_FRONTEND
     → IMPLEMENT → BUILD_VERIFY → VISUAL_REVIEW
     → E2E_VERIFY → TEST → ARCHIVE → DONE
```

| # | 阶段 | 谁做 | 产出概要 |
|---|------|------|----------|
| 0 | `INIT` | 编排器 | 脚手架、`state.json`、意图分析（自动流转） |
| 1 | `ANALYSE_PRODUCT` | 4 人产品团队 | 用户故事、业务规则、基线对比、视觉分析 |
| 2 | `CLARIFY_PRODUCT` | 编排器 | 有 pending 问题才进入，否则跳过 |
| 3 | `ANALYSE_TECH` | 4 人技术团队 | 按 `repos[]` 扫描、方案、分端拆解 |
| 4 | `CLARIFY_TECH` | 编排器 | 同上，无问题则跳过 |
| 5 | `ARCHITECT_BACKEND` | 全局架构师 + 领域架构师 ×N | 领域拆分、DB / API 契约 |
| 6 | `CLARIFY_ARCH_BACKEND` | 编排器 | 澄清或跳过 |
| 7 | `ARCHITECT_FRONTEND` | 前端架构师 | Web / 小程序架构 |
| 8 | `CLARIFY_ARCH_FRONTEND` | 编排器 | 澄清或跳过 |
| 9 | `IMPLEMENT` | 各端开发 Agent 并行 | 按领域所有权写代码；前端可由 D2C 直通 |
| 10 | `BUILD_VERIFY` | 后端 / Web / 小程序验证 | **P0 门禁**，失败可只重跑失败端 |
| 11 | `VISUAL_REVIEW` | 视觉验收 | **P1 可选**，无设计稿自动跳过 |
| 12 | `E2E_VERIFY` | 链路验证 | 跨服务运行时依赖（7 维） |
| 13 | `TEST` | 测试工程师 | 3 层测试方案并执行 |
| 14 | `ARCHIVE` | 归档 + 事实校对 | 知识提取、知识仓 push、TAPD/企微 |
| 15 | `DONE` | — | 终态；只有 archiver 能写入 |

四个 `CLARIFY_*` 仅在对应 `*-clarify.json` 存在且有 `pending` 问题时进入。

**防漂移硬约束**：

- `BUILD_VERIFY` 通过 ≠ 做完，后面还有视觉 / E2E / 测试 / 归档
- `TEST` 通过 ≠ 做完，必须进 `ARCHIVE`
- 只有 `ARCHIVE` 的 archiver 能把 `currentPhase` 写成 `DONE`

### 4.4 每个阶段的三步节奏

```
Preview（展示计划，等你说「继续 / 调整」）
    → Execute（调度 Agent，写产物，更新 state.json）
    → Summary（展示产出与质量信号，等你确认流转）
```

INIT 例外：自动执行并流转。

### 4.5 知识怎么在流程里流动

```
/flow-import（一次性冷启动）
    文档收集 → 代码画像 → 知识标准化
    写入团队知识仓（draft）

/flow-run 每次 INIT
    git pull 知识仓，把查询入口注入 Agent

各分析 / 架构 / 实现 / 验证阶段
    Agent 按预算主动查 catalog → 条目

ARCHIVE
    提取新知识 → 提升判定（项目特有留 Layer 3，通用技术/业务升到团队仓）
    fact-checker 做符号级代码事实校对
    git push → 下一个需求自动站在前人肩上
```

---

## 5. 支持哪些功能

### 5.1 用户命令（9 个）

| 命令 | 做什么 |
|------|--------|
| `/flow-run` | 启动或续跑交付工作流（PRD / 文字 / 直接启动） |
| `/flow-import` | 已有项目冷启动：从 Git / TAPD / iWiki / 本地文档 / 口述导入知识 |
| `/flow-status` | 看所有需求的阶段、进度、风险（不启动编排器） |
| `/flow-upgrade` | 对比远程引擎版本，选择性更新双平台目录 |
| `/team-init` | 初始化 `.ai-team/project.yaml`，连接知识仓，绑定 `repos[]` |
| `/knowledge` | 知识维护：`status` / `lint` / `sync` / `query` / `add` / `fact-check` / `promote` |
| `/evolve` | 从 Bug 反推是哪个 Agent 环节漏了，只记建议不改引擎 |
| `/evolve-apply` | Owner 审阅并落地 `/evolve` 的 pending 建议 |
| `/guard` | 守卫模式：每步确认后再执行 |

### 5.2 Skills（按用途）

**核心工作流**

| Skill | 能力 |
|-------|------|
| `workflow-orchestrator` | 16 阶段编排、Agent 调度、流转守卫、断点恢复 |
| `knowledge-evolution` | 知识提取 / 存储 / 查询 / 衰减 / 提升 |
| `figma-d2c` | Figma → 代码，16 步检查点；可直通或嵌入工作流 |
| `prd-creator` | 苏格拉底式提问，生成结构化 PRD |
| `quality-guardian` | 质量看板、回退分析、趋势 |
| `team-hub` | 多需求协同：角色看板、瓶颈、资源冲突 |

**集成**

| Skill | 能力 |
|-------|------|
| `tapd-toolkit` | TAPD 图片/附件上传下载（补 MCP 缺口） |
| `git-push-helper` | 暂存 → 生成 commit message → push / MR / 通知 |
| `send-flow-message` | 企微群通知（卡片 / Markdown） |
| `iwiki-operation` | iWiki 文档读写（仅 CodeBuddy） |
| `mcp-setup-guide` | 引导配置 TAPD / iWiki / Figma 等 MCP |

**元能力**

| Skill | 能力 |
|-------|------|
| `capability-router` | 意图不清时路由到合适 Skill |
| `model-router` | 按复杂度 / 上下文 / 成本选模型 |
| `skill-learner` / `skill-creator` | Skill 评估、创建与改进 |
| `token-budget-manager` | Token 消耗追踪与预警 |
| `pdf` / `docx` | 通用文档处理 |

`.codebuddy/` 18 个 Skill，`.claude/` 17 个（`iwiki-operation` 为 CodeBuddy 单边）。

### 5.3 交付能力边界

工作流覆盖的典型交付：

- 新产品需求（完整分析 → 多端实现 → 验证 → 归档）
- 已有功能修改（带历史基线对比）
- 缺陷修复（可缩短架构阶段）
- 技术重构
- Figma 设计稿转代码（standalone 直通 或 embedded 嵌入 IMPLEMENT）
- 单仓单体 或 多仓微服务（同一套 `repos[]`）
- 后端（含 Java 专版架构师）+ Web + 小程序

集成点（有则自动用，无则降级跳过）：TAPD、iWiki、Figma、企微通知、LSP 诊断。

### 5.4 它不做什么

- 不提供独立 Web 控制台或常驻服务
- 不替代 Git / CI / 代码评审平台
- 不要求业务项目引入 Python 运行时（`scripts/` 只给引擎维护者）
- 不会在未拷贝 `.codebuddy/` / `.claude/` 的空仓库里凭空出现 `/flow-run`

---

## 6. 使用方式

### 6.1 环境前提

1. 用 **CodeBuddy IDE** 或 **Claude Code** 打开业务项目工作区根目录
2. 工作区根下已有 `.codebuddy/` 与 `.claude/`（见 §6.2）
3. 建议配置 TAPD / iWiki / Figma 等 MCP（可用 `/mcp-setup-guide`）；不是硬前置

### 6.2 安装到业务项目（必须 copy）

在业务项目里对 Agent 说：

```
请帮我从远程仓库拉取 ai-team 工作流系统到当前工作区。按顺序执行：

1. cd {当前工作区根目录}
   git clone --depth 1 git@git.woa.com:Agentic-CE-Infra/ur-ai-team.git .ai-team-install

2. cp -r .ai-team-install/.codebuddy ./
   cp -r .ai-team-install/.claude ./

3. rm -rf .ai-team-install

4. 验证：检查 .codebuddy/skills/、.codebuddy/commands/、
   .claude/skills/、.claude/commands/ 是否存在。
```

同事加入同一项目：把这两个目录纳入业务仓（或按团队约定同步）即可，**不要**把本引擎仓的 `scripts/`、`meta/` 带过去。

### 6.3 每个项目做一次初始化

```
/team-init
```

会引导你：

- 连接团队知识仓库 Git 地址（或本地新建）
- 填写业务领域、技术栈
- 绑定 `repos[]`（单仓填一个 `./`，多仓把各微服务路径列全）

产物：工作区根的 `.ai-team/project.yaml`。已初始化再跑会展示当前配置，改仓请用 `/flow-import`，彻底重来需先删该文件。

### 6.4 已有代码库：先导入再开发（推荐）

```
/flow-import
```

三人管道：收集文档 → 代码画像 → 标准化知识。产出写入团队知识仓，后续 `/flow-run` 的分析阶段会消费。

### 6.5 日常开发

```
/flow-run                              # 从上下文或已有 PRD 选
/flow-run docs/prd/my-requirement.md   # 指定 PRD
/flow-status                           # 看进度，不启动编排器
```

每个非 INIT 阶段都会先给你看计划。回复「继续」才执行；Summary 后再决定进下一阶段或回退。

中途关掉对话：下次再 `/flow-run`，编排器读 `state.json` 从当前阶段恢复。

### 6.6 知识与复盘

```
/knowledge status        # 知识库概况
/knowledge lint          # 健康检查（含活跃度抑制、事实漂移）
/knowledge sync          # 与团队知识仓双向同步
/knowledge query 关键词  # 查询
/knowledge fact-check    # 重构后立刻做代码事实校对
/knowledge add           # 手工补一条
/knowledge promote       # 手工做提升判定

/evolve                  # 从 Bug 反推流水线哪个环节该改（只记建议）
/evolve-apply            # Owner 落地建议
```

### 6.7 升级引擎

```
/flow-upgrade
```

浅克隆远程 `ur-ai-team`，按 Skill / Command / Rule 模块对比本地 `.codebuddy/` 与 `.claude/`，你选全量或按模块更新。本地多出来的自定义文件不会被覆盖。

### 6.8 高风险操作

```
/guard
```

之后 AI 不直接改文件、不直接跑命令，每步先提案再等你批。

---

## 7. 安装后业务项目长什么样

**单仓：**

```
你的业务仓库根/
├── .codebuddy/              ← 从引擎仓拷来（运行时）
├── .claude/                 ← 从引擎仓拷来（运行时）
├── .ai-team/project.yaml    ← /team-init 生成
├── docs/
│   ├── prd/                 ← PRD
│   ├── knowledge-base/      ← 项目级知识（Layer 3）
│   └── workflows/           ← 每个需求一份 state + 产物
├── src/                     ← 你的代码
└── ...
```

**多仓（IDE 打开的是父目录，不一定是某个 Git 根）：**

```
工作区根/
├── .codebuddy/  +  .claude/
├── .ai-team/project.yaml
├── project-docs/            ← type=docs，承载 docs/workflows
├── ad-service/              ← type=backend
├── creative-service/        ← type=backend
└── ad-frontend/             ← type=frontend
```

---

## 8. 两种角色，两套用法

| | 业务项目使用者 | 引擎维护者 |
|--|----------------|------------|
| 打开哪个仓 | 自己的业务项目 | 本仓库 `ur-ai-team` |
| 要什么文件 | 只需 `.codebuddy/` + `.claude/` | 完整 clone（含 `scripts/`、`meta/`） |
| 日常命令 | `/flow-run`、`/knowledge`、`/team-init` … | 改 Skill 后跑体检脚本 |
| 升级方式 | `/flow-upgrade` | 改完推远程，使用者再 upgrade |
| 必读 | 本文件 + [README.md](./README.md) | [ARCHITECTURE.md](./ARCHITECTURE.md) + [CLAUDE.md](./CLAUDE.md) |

维护者改引擎时：先改 `.codebuddy/`（前线），再按平台方言翻译到 `.claude/`，最后跑 `python3 scripts/consistency_check.py`。这与业务项目里「拷两个目录就能用」是两件事。

---

## 9. 还想看更细的

| 问题 | 文档 |
|------|------|
| 安装口令、命令/Skills 清单 | [README.md](./README.md) |
| 阶段表、Agent 编制、知识模型、state.json 字段 | [ARCHITECTURE.md](./ARCHITECTURE.md) |
| 交互式看阶段 / Agent / Rule | 浏览器打开 [docs/workflow-visualization.html](./docs/workflow-visualization.html) |
| 引擎体检、DSL、镜像脚本 | [scripts/README.md](./scripts/README.md)、[meta/README.md](./meta/README.md) |
